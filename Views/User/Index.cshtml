@model List<User>

@{
    ViewData["Title"] = "Usuários";
}
<a asp-controller="User" asp-action="Create">Criar Usuário</a>
<ul>
    @foreach (var user in Model)
    {
        <li>
            <p>@user.UserId</p>
            <p>@user.Name</p>
            <p>@user.Email</p>
            <p>@user.Phone</p>
            <p>@user.Type.ToString()</p>
            <a asp-action="Profile" asp-route-id="@user.UserId">Perfil</a>
        </li>
    }
</ul>